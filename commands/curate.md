# Curate Command

内容策展：采集订阅源新内容 → 筛选 → 分类 → 关联 know-how → 生成 digest。

---

## 使用场景

- 查看订阅源最近有什么值得看的内容
- 定期生成内容摘要
- 追踪特定话题在信息流中的动态

---

## 输入

```
/curate                        # 全量采集（X + Blogs）
/curate --blogs                # 仅博客 RSS
/curate --x                    # 仅 X 账号
/curate --topic [keyword]      # 检索已采集数据中特定话题
/curate --since [YYYY-MM-DD]   # 指定起始日期
```

示例：
- `/curate` — 全量采集和筛选
- `/curate --blogs` — 只看博客更新
- `/curate --x` — 只看 X 账号动态
- `/curate --topic "Context Engineering"` — 追踪特定话题

---

## 执行步骤

1. 加载 `teammate-content-curator` 角色
2. 读取 `feed-registry.yaml` 和 `feed-state.json`
3. 按参数执行采集：
   - `--blogs` → 调用 `rss-feed-monitor` Skill
   - `--x` → 调用 `x-timeline-monitor` Skill
   - 无参数 → 两者都执行
   - `--topic` → 跳过采集，在已有数据中检索
4. 调用 `content-classification` Skill 分类和过滤
5. 关联 know-how/，标注 🆕/✅/⚠️/🔄
6. 生成 digest 报告
7. 展示 Highlights 给 Ivan

---

## 输出

报告文件：`outputs/reports/content-curator/YYYY-MM/[AUTO]_content-digest_{YYYY-MM-DD}.md`

直接展示：Highlights 区域内容（必读项）

---

## 反馈

Ivan 对 digest 内容的反馈会被 content-curator 记录：
- "好" / "有用" → 提升该源和该类内容的评分
- "没用" / "噪音" → 降低评分，收紧过滤
- "漏了 XX" → 扩大采集范围

---

*v1.0 | 2026-02-12*
